apiVersion: v1
kind: Service
metadata:
  name: '{{ .Release.Name }}'
  labels:
    app: '{{ .Release.Name }}'
spec:
  selector:
    app: '{{ .Release.Name }}'
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: web
      name: web
