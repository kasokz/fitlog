# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Offline data storage | capacitor-community/sqlite | Structured relational data needs SQL, not key-value. Capacitor Preferences only for app settings. | No |
| D002 | M001 | arch | Data model sync readiness | UUID PKs + timestamps + soft delete | Enables future cloud sync (M004) without schema migration. crypto.randomUUID() works offline. | No |
| D003 | M001 | pattern | Workout logging UX | Tap-tap-done with pre-fill from last session | Minimum taps is the #1 UX priority. Pre-fill from last session for same program day. | No |
| D004 | M001 | data | RIR tracking granularity | Per set (not per exercise) | User trains with RIR as a first-class signal on every working set. Drives M002 progression logic. | No |
| D005 | M001 | pattern | Set type taxonomy | Warmup, Working, Drop, Failure | Only working sets count toward progression analytics. Prevents data pollution. | Yes — if users request more types |
| D006 | M001 | scope | Rest timer behavior | Optional/manual, no auto-start | Timer available but not intrusive. Some users track rest, some don't. | Yes — if user feedback demands auto-start |
| D007 | M001 | data | Mesocycle model | Full mesocycles with week blocks + deload scheduling | Programs define week count and deload position. Deload auto-adjustment is M002 scope. | No |
| D008 | M001 | scope | Body tracking scope | Body weight only, no photos or measurements | Keep it simple. Photos and measurements deferred (R037). | Yes — if demand appears |
| D009 | M001 | arch | Monetization model | Freemium with granular feature packs | Free: core logging + basic history. Premium: analytics, AI suggestions, premium templates. | Yes — pricing/packaging revisable |
| D010 | M001 | scope | Platform targeting | iOS + Android simultaneously | Both platforms from M001. No web-only phase. | No |
| D011 | M001 | pattern | Progression algorithm signal | RIR-driven (not rep range ceiling) | User chose RIR as primary signal. Consistently RIR 2-3 across sets → suggest weight increase. | No |
| D012 | M001 | pattern | Deload scheduling | Scheduled in mesocycle + auto-adjusted (M002) | Deload position defined in program. M002 auto-reduces weight/volume to ~60%. | No |
| D013 | M001 | scope | Social features | Not now — deferred post-M004 | Solo app focus. Social adds complexity without clear ROI at this stage. | Yes — revisit after M004 |
| D014 | M001 | scope | Cloud sync depth | Sync-ready infra, not just data model | User wants actual sync mechanism built (M004), not just schema readiness. | No |
| D015 | M001 | pattern | Onboarding flow | Pick a starter template, not build-your-own | Fastest path to first workout. Templates: PPL, Upper/Lower, Full Body. | Yes — if users want guided creation |
| D016 | M001/S01 | arch | SQLite plugin choice | @capgo/capacitor-fast-sql (supersedes D001) | Higher-performance alternative using HTTP protocol to bypass Capacitor bridge serialization. Cleaner API (FastSQL + SQLConnection). Web fallback via sql.js + IndexedDB. Same Capgo team. v8.0.23 available in references/. | No |
| D017 | M001/S01 | pattern | Database abstraction | Thin repository over raw SQL, no ORM | Repository classes encapsulate SQL, validate with Zod, return typed objects. No drizzle/ORM overhead — complexity not justified for the data model size. Plugin interface used directly (not HTTP SQLConnection on web). | Yes — if schema grows significantly |
| D018 | M001/S01 | pattern | Schema migration strategy | schema_version table with manual version checks | Simple version-tracked DDL on connect. Version 1 = initial schema. Future slices increment version and add migration SQL. No migration framework. | Yes — if migration count exceeds ~10 |
| D019 | M001/S02 | data | Program data model | 4-table normalized: programs, training_days, exercise_assignments, mesocycles | Normalized over JSON-embedded to support S03 per-assignment queries, S05 template creation per-entity, and sort_order reordering. Programs are templates; mesocycles are instances with week tracking. | No |
| D020 | M001/S02 | pattern | Exercise reordering in training days | sort_order integer + up/down buttons | Simple reordering with sort_order column. Gaps on delete are fine. dnd-kit drag-and-drop deferred to S06 polish. | Yes — upgrade to dnd-kit in S06 |
| D021 | M001/S02 | data | Mesocycle cardinality | One active mesocycle per program (table supports multiple) | Table has program_id FK allowing multiple rows, but UI only creates/shows one. S03/S05 can extend to sequential mesocycles later. | Yes — extend to multiple in S03+ |
| D022 | M001/S02 | pattern | Soft-delete cascade policy | No auto-cascade on program soft-delete | Soft-deleting a program does NOT cascade to training days or assignments. S03 workout sessions may reference a training day FK. Entities are independently soft-deletable. | No |
| D023 | M001/S03 | pattern | Active workout state management | In-memory $state with DB flush on set confirm | Stepper taps update in-memory state only. DB writes happen on set confirm (checkbox), add set, or remove set. Prevents rapid-tap DB writes causing lag. | No |
| D024 | M001/S03 | pattern | Set removal during active workout | Hard-delete (not soft-delete) for in-progress sets | Soft-delete is overhead for in-progress edits. Completed sessions use soft-delete. During active workout, removing a set is an immediate hard DELETE. | Yes — if undo support needed |
| D025 | M001/S03 | pattern | Duration timer accuracy across backgrounding | started_at timestamp + calculated elapsed | setInterval pauses on iOS/Android background. Duration is always computed as `now - started_at`, interval just updates display. No accumulated tick counter. | No |
| D026 | M001/S03 | pattern | Superforms usage in workout logging | Direct repository calls for set editing, not Superforms | Superforms SPA mode is for traditional form submits. Active workout set editing is rapid inline mutation (stepper taps). Superforms appropriate for start/finish confirmation flows if needed, but not for per-set editing. | Yes — if form patterns change |
| D027 | M001/S03 | data | Concurrent workout sessions | Only one in_progress session at a time | createSession checks for existing in_progress sessions and throws if one exists. UI offers resume option. Prevents data confusion from multiple active workouts. | Yes — if multi-workout support requested |
| D028 | M001/S03 | pattern | SQLite ORDER BY tiebreaker | rowid DESC as secondary sort for timestamp-based ordering | Same-millisecond timestamps cause non-deterministic ordering in SQLite. Adding `rowid DESC` as tiebreaker guarantees the most recently inserted row wins. Applied to getLastSessionForDay and getMesocycleByProgramId. | No |
| D029 | M001/S04 | data | Body weight entry cardinality | One entry per date, upsert on conflict | UNIQUE(date) constraint on body_weight_entries. INSERT OR REPLACE semantics: logging weight for a date that already has an entry updates it. Simplest model — no multi-entry-per-day ambiguity. | Yes — if users want AM/PM tracking |
| D030 | M001/S04 | data | Body weight storage unit | Kilograms (REAL), no unit column | Store weight_kg as REAL. Matches existing workout_kg_unit pattern. Unit conversion (kg/lbs) deferred to S06 polish or later. | Yes — add unit preference in settings |
| D031 | M001/S04 | data | Body weight unique constraint with soft-delete | Partial unique index (WHERE deleted_at IS NULL) instead of UNIQUE(date) | Table-level UNIQUE(date) conflicts with soft-delete: deleted row holds the date, blocking new INSERT. Partial unique index enforces one-active-entry-per-date while allowing soft-deleted rows to coexist. Repository uses SELECT+conditional INSERT/UPDATE instead of INSERT OR REPLACE. | No |
| D032 | M001/S05 | pattern | Exercise name resolution for templates | ExerciseRepository.getByName() exact match, not LIKE | Templates reference seed exercises by exact name string. Using LIKE via search() risks false positives (e.g., "Curl" matching "Barbell Curl"). Added getByName() with WHERE name = ? for deterministic resolution. | No |
| D033 | M001/S05 | pattern | Template creation fail-fast strategy | Resolve ALL exercise names before creating any DB entities | If any template exercise name fails to resolve, abort entirely. No partial program creation. Prevents orphan programs with missing exercises. Error lists all missing names. | No |
| D034 | M001/S05 | pattern | Onboarding state storage | @capacitor/preferences, not SQLite | Onboarding completion is app-level boolean state, not relational data. Preferences is already in dependencies and is the right tool. Simple get/set/remove pattern. | No |
| D035 | M001/S05 | pattern | Onboarding skip option | "Skip" button available, marks onboarding complete without creating a program | D015 says "Pick a starter template, not build-your-own" for onboarding, but users who want manual program creation shouldn't be blocked. Skip navigates to home where programs page supports manual creation. | Yes — if analytics show skip is overused |
| D036 | M001/S06 | pattern | Primary navigation pattern | Bottom tab bar with 5 tabs replacing home-grid | Mobile apps need persistent navigation. Bottom tab bar is the standard pattern. Home page grid becomes redundant — home becomes dashboard or redirects to programs. Tabs: Programs, Exercises, History, Body Weight, Settings. | Yes — if tab count grows beyond 5 |
| D037 | M001/S06 | pattern | Haptics integration approach | Fire-and-forget calls, no await | Haptic calls must never block UI thread. All calls are void fire-and-forget with try/catch in the service layer. Debug logging in dev mode for verification. | No |
| D038 | M001/S06 | pattern | App identity | appId: com.fitlog.app, appName: FitLog | Reverse-domain ID for both stores. Must be set before native project scaffold (cannot easily change later). | No |
| D039 | M001/S06 | pattern | Native project git strategy | Commit ios/ and android/ to repo | Capacitor native projects should be committed since they contain configuration, plugin registrations, and platform-specific settings that must be version-controlled. Unlike node_modules, these are not fully reproducible from cap add alone. | Yes — if monorepo constraints change |
| D040 | M001/S07 | pattern | Paraglide locale strategy for Capacitor SPA | `["localStorage", "preferredLanguage", "baseLocale"]` | Cookie strategy doesn't work for SPA (no server to set/read cookies). localStorage persists user's manual choice, preferredLanguage auto-detects device language via navigator.languages, baseLocale is the final fallback. Order ensures manual override wins over device detection. | Yes — if SSR is enabled later |
| D041 | M001/S07 | pattern | Zod refinement i18n approach | Use `m.*()` calls directly in Zod `.refine()` message field | Schemas are defined inside Svelte `<script>` blocks, so `m.*()` is evaluated at validation time (not module load). No need for error message override layer or schema factory pattern. | Yes — if schemas move to shared modules |
| D042 | M002 | arch | Analytics service layer location | `src/lib/services/analytics/` as pure functions + repository | Analytics computation separated from UI. Pure functions (1RM, PR detection, volume, progression) are independently testable. Repository handles SQL queries. No analytics data stored in separate tables — computed on read from existing workout_sets. | Yes — if performance requires materialized aggregates |
| D043 | M002 | pattern | 1RM estimation formula strategy | Epley for reps 1-10, null for reps >10 | Epley `w*(1+r/30)` is more accurate at moderate rep ranges. Both Epley and Brzycki degrade above 10 reps (20%+ error). Returning null for high-rep sets avoids misleading estimates. | Yes — add Brzycki as alternative |
| D044 | M002 | pattern | PR categories | Three separate types: weight_pr, rep_pr, e1rm_pr | Weight PR = heaviest single working set. Rep PR = most reps at same or higher weight. e1RM PR = highest estimated 1RM. Tracking separately prevents phantom PR confusion (e.g., higher e1RM from lighter weight × more reps). Weight PR shown prominently, e1RM secondary. | No |
| D045 | M002 | pattern | Progression suggestion thresholds | >=2 consecutive sessions, avg working-set RIR >=2, min 3 working sets per session | Conservative defaults to avoid aggressive suggestions. Per-exercise, filters to completed working sets only. Weight increment rounded per equipment type (barbell: 2.5kg, dumbbell: 2kg, cable/machine: 2.5kg). Thresholds stored as constants, easy to tune. | Yes — tune based on user feedback |
| D046 | M002 | pattern | Deload auto-adjustment mechanism | Modify pre-fill in getLastSessionForDay when mesocycle_week == deload_week_number | Hooks into existing pre-fill flow (D003). Deload reduces weight to ~60% and can reduce set count. Applied at session creation time, not retroactively. User sees deload-adjusted values and can override. | No |
| D047 | M002 | pattern | Analytics navigation | Dashboard accessible via History tab sub-route `/history/analytics` + per-exercise analytics from exercise detail | Avoids adding a 6th bottom tab (D036 constraint). History tab is the natural home for analytics. Per-exercise strength curves accessible from exercise detail view. Dashboard linked from History page header. | Yes — if dedicated tab added later |
| D048 | M002 | pattern | Freemium gate implementation | Local feature-flag service with @capacitor/preferences | No server needed. `premium.ts` service reads premium status from Preferences. M003 will wire IAP verification to flip the flag. Free tier: basic PR list, last-4-weeks summary. Premium: full charts, extended history, progression suggestions. | Yes — M003 will add real IAP verification |
| D049 | M002 | arch | Chart library | LayerChart v2 (`layerchart@next`) via shadcn-svelte chart wrappers | shadcn-svelte chart components already exist in packages/ui wrapping LayerChart. Pin version to avoid pre-release breakage. Install as devDependency in apps/mobile. | Yes — if LayerChart v2 has breaking issues |
| D050 | M002 | data | Analytics query performance indexes | Schema v5 adds composite index on workout_sets(exercise_id, set_type, completed) WHERE deleted_at IS NULL | Analytics queries filter on exercise_id + set_type='working' + completed=1 for every calculation. Without covering index, full table scans degrade as data grows. Schema v5 is a minimal migration (index only, no table changes). | No |
| D051 | M002/S01 | pattern | Analytics working-set filter enforcement | Single source of truth in AnalyticsRepository, defense-in-depth in services | All analytics queries in AnalyticsRepository filter `set_type='working' AND completed=1 AND deleted_at IS NULL`. Analytics services additionally filter inputs as defense-in-depth. Prevents data pollution from warmup/drop/failure/incomplete sets across all analytics computations. | No |
| D052 | M002/S01 | pattern | Progression advisor bodyweight exercise handling | Skip bodyweight exercises entirely, return null | Exercises with `equipment='bodyweight'` (pull-ups, dips, push-ups) don't use external weight. Progression advisor returns null immediately for these. Rep-based progression suggestions deferred to user feedback. | Yes — add rep suggestions if demanded |
| D053 | M002/S01 | pattern | Deload weight rounding strategy | Round to nearest 2.5kg (standard plate increment) | Deload weight reduction rounds to nearest 2.5kg regardless of equipment type. Simplest practical approach — plate math always works in 1.25kg increments (2.5kg per side). Avoids equipment-specific rounding complexity in deload context. | Yes — if per-equipment rounding needed |
| D054 | M002/S01 | pattern | Volume aggregation muscle group scope | Primary muscle group only, not secondary | Volume aggregation counts full volume for the exercise's primary muscle group only. Secondary muscle group volume tracking adds complexity with unclear benefit. Start simple, extend later if users request per-muscle-group detail including secondary contributions. | Yes — extend to secondary if demanded |
| D055 | M002/S02 | pattern | Dashboard data service as chart intermediary | Dedicated `dashboardData.ts` transforms analytics outputs to chart-ready shapes | Chart components stay thin — they receive pre-shaped data (Date objects, grouped aggregates) rather than calling analytics services directly. Single transformation layer converts YYYY-MM-DD strings to Date objects, groups best e1RM per date, and handles Promise.all parallelism. | Yes — if performance requires caching |
| D056 | M002/S02 | pattern | Dashboard tab organization | Tabs component with 4 sections: Strength, Volume, Body Weight, Frequency | Each chart type gets its own tab for clean separation. Avoids a long scrollable page with all charts stacked. Exercise picker and time-range selector are global (outside tabs) so changing them updates whichever tab is active. | Yes — may switch to scroll layout if UX feedback prefers it |
| D057 | M002/S02 | pattern | Strength chart data grouping | Best e1RM per date, not all-sets scatter | Multiple working sets per session would create cluttered overlapping points. Grouping by date and taking the highest e1RM produces a clean trend line. Uses `estimateOneRepMax` per set then max per date. | No |
| D058 | M002/S03 | pattern | Session PR detection architecture | Thin orchestration service `detectSessionPRs()` bridging S01 engine and workout UI | Separates orchestration concerns (session-level queries, current-session exclusion, parallelization, error resilience) from pure detection logic (`detectPRs`). Workout page passes in-memory exercise groups to avoid redundant DB reads for exercise names. | No |
| D059 | M002/S03 | pattern | PR celebration non-blocking strategy | PR detection failure never blocks workout completion | `detectSessionPRs()` returns empty result on failure (never throws). The celebration toast is a nice-to-have; the session is already marked complete before PR detection runs. User sees normal success toast on detection failure. | No |
| D060 | M002/S03 | pattern | PR history page location | `/history/prs` sub-route, accessible via Trophy icon on History page | Follows D047 pattern (analytics accessible via History tab sub-routes). Trophy button placed alongside the existing BarChart3 analytics button. Respects D036 (5-tab nav constraint — no new tab). | Yes — could move to a Drawer or tab within history |
| D061 | M002/S05 | pattern | Deload banner scope | Page-level banner above all exercise cards, not per-exercise | Unlike progression suggestions (per-exercise), deload affects the entire session uniformly. A single page-level banner avoids N duplicate banners and is cleaner UX. Uses amber color theme to distinguish from green progression banners. | Yes — if per-exercise deload detail needed |
| D062 | M002/S05 | pattern | Deload banner detection source | Session's stored `mesocycle_week` vs mesocycle's `deload_week_number` | Uses creation-time snapshot (`session.mesocycle_week`) rather than live mesocycle state. Stable across app resume, immune to mesocycle edits after session creation. Gracefully skips if mesocycle is null or deleted. | No |
| D063 | M002/S06 | pattern | Free-tier analytics scope | Strength tab + Frequency tab + 30d time range + top 3 exercise PRs | Free users see Strength chart (1RM over time) and FrequencySummary (total sessions + avg/week) as their "last-4-weeks summary." Volume and Body Weight tabs are premium. Time range locked to 30d. PR history limited to 3 exercises. Research option (a) chosen: FrequencySummary serves as the free summary view. | Yes — if free tier scope needs adjustment |
| D064 | M002/S06 | pattern | Freemium gate enforcement location | Page-level gate before data loading, not component-level | Premium check happens at route page level (`$effect` with `isPremiumUser()`) before initiating data loads. Prevents loading premium data then retroactively hiding it. Components (charts, ProgressionBanner) don't know about premium — they simply don't receive data. | No |
| D065 | M002/S06 | pattern | Non-gated features explicit list | Deload banners, PR celebration toasts, ExercisePRSection (current bests) | These are core training features, not premium analytics. Deload is safety. PR celebration is motivation. ExercisePRSection shows basic "current best" per exercise. Gating these would make the free tier feel punitive. | No |
| D066 | M003 | arch | IAP plugin choice | `@capgo/native-purchases` over RevenueCat | Free (zero revenue share), same Capgo vendor as existing DB plugin, uses StoreKit 2 + Play Billing 7.x directly, Capacitor 8 SPM support. RevenueCat adds backend dependency, revenue share (~1% after $2.5M), and is overkill without server-side validation (M004 scope). | Yes — if plugin proves incompatible with Cap 8 |
| D067 | M003 | pattern | Premium product model | Granular product tracking replacing single boolean | Current `premium.ts` stores one boolean. M003 evolves it to track specific products: analytics subscription (annual/monthly) and template pack (one-time). `canAccessFeature()` checks product ownership, not just "is premium." Supports future product additions without schema changes. | No |
| D068 | M003 | pattern | Pricing model | Annual subscription + one-time template pack | Annual subscription for analytics/progression features (most common in fitness apps). One-time purchase for premium template pack. Architecture supports adding monthly/lifetime later. Prices loaded dynamically from stores (Apple requirement). | Yes — add monthly/lifetime based on conversion data |
| D069 | M003 | pattern | Premium template naming | Generic descriptive names, not trademarked methodology names | Use "Periodized Strength 531", "Linear Progression LP", "Tiered Volume Method" etc. instead of "Wendler 5/3/1", "nSuns", "GZCL". Avoids trademark/C&D risk while clearly describing the methodology. | No |
| D070 | M003 | pattern | Store listing localization | German (primary) + English for both stores | Matches existing i18n setup (de base locale, en secondary). Both App Store and Play Store support localized listings. No emojis in any metadata text (AGENTS.md constraint). | No |
| D071 | M003 | pattern | Purchase validation scope | Local-only, no server-side receipt validation | M003 scope explicitly excludes server-side validation (M004). Check `isActive` on iOS, `purchaseState` + `isAcknowledged` on Android. Revalidate on app launch via `getPurchases()`. Acceptable risk for a fitness app — determined bypass requires technical effort disproportionate to the product's price. | Yes — M004 adds server-side validation |
| D072 | M003/S01 | pattern | Purchase plugin wrapper logging convention | `[PurchasePlugin]` prefix with level-based semantics | Debug level for web/platform skips, info level for success, error level for failures. Matches `[Premium]` and `[Haptics]` prefixed log patterns. Product IDs logged, purchase tokens/receipts never logged. | No |
| D073 | M003/S01 | pattern | Purchase plugin wrapper error strategy | Catch-and-return safe defaults, never throw | Unlike haptics (fire-and-forget void), purchase functions return typed results. On error: `isBillingSupported()` → false, list functions → [], single-item functions → null. Callers don't need try/catch. | No |
| D074 | M003/S01 | pattern | StoreKit testing configuration for local dev | `Products.storekit` file with 3 products, manual Xcode scheme selection | StoreKit config enables sandbox testing without App Store Connect product setup. Developer must select the `.storekit` file in Xcode scheme → Run → Options → StoreKit Configuration. Not automatable in headless builds. | Yes — replace with App Store Connect products when store is configured |
| D075 | M003/S02 | pattern | Purchase state persistence model | JSON-serialized `Map<productId, PurchasedProduct>` in Preferences under `purchased_products` key | Replaces single boolean `premium_status`. JSON map supports multiple products, per-product metadata (expiry, type, active status). Dev override key (`premium_status`) coexists — checked first for backward compat. Corrupted JSON falls back to empty map (free user). | No |
| D076 | M003/S02 | pattern | Transaction active status abstraction | `isTransactionActive()` with platform-specific branching | iOS subscriptions: check `isActive === true`. Android subscriptions: check `purchaseState === '1'`. iOS INAPP: presence sufficient. Android INAPP: `purchaseState === '1'`. Handles missing fields gracefully with fallback heuristics. | No |
| D077 | M003/S02 | pattern | Revalidation timing strategy | App launch (mount) + resume only, not per-navigation | Calling `getPurchases()` on every page load is excessive. Mount + resume covers app cold start and background return. Page-level checks read persisted state (fast, no network). First-render may show stale state from previous session — acceptable trade-off. | Yes — add periodic timer if subscription expiry timing matters more |
| D078 | M003/S02 | pattern | Web revalidation no-op behavior | Skip state overwrite when `getPurchases()` returns empty on web | `getPurchases()` returns `[]` on web per D073. Revalidation must not overwrite existing persisted state with empty array on web, or dev-toggle + persisted state would be wiped. On native, empty `getPurchases()` result after a previous non-empty state legitimately means subscriptions expired — overwrite is correct. Differentiate by checking `Capacitor.isNativePlatform()`. | No |
| D079 | M003/S03 | pattern | Paywall component pattern | vaul-svelte Drawer (not Dialog, not route) | Codebase uses Drawer for all bottom-up mobile interactions (6 existing usages). Preserves page context — user stays where they are. Dialog is for confirmations. A dedicated route would break contextual flow. | No |
| D080 | M003/S03 | pattern | Post-purchase page reactivity | Explicit re-check via `isPremiumUser()` callback, not reactive store | Current architecture uses async `isPremiumUser()` in `$effect`. After purchase, PaywallDrawer fires `onpurchasecomplete` callback → parent page re-runs the premium check → UI updates. Simpler than introducing a global reactive store. Acceptable because the drawer close naturally triggers a re-render context. | Yes — introduce reactive store if more pages need instant purchase updates |
| D081 | M003/S03 | pattern | Purchase cancellation UX | No error toast on `null` return from `purchaseProduct()` | Plugin returns `null` for both user cancellation and real errors (D073). Since user cancellation is the common case, showing an error toast would be misleading. Show nothing — user returns to paywall and can retry or dismiss. Success toast only on completed transaction. | No |
| D082 | M003/S03 | pattern | Paywall legal link strategy | Placeholder URLs in constants file, real URLs in S05 | Apple requires Privacy Policy and Terms of Service links in paywall. URLs don't exist yet. Store placeholder constants (`https://fitlog.app/privacy`, `https://fitlog.app/terms`) that S05 will make real. Acceptable for sandbox testing but must be updated before store submission. | Yes — S05 must replace with real URLs |
| D083 | M003/S04 | pattern | Template browsing UI placement | Secondary "From Template" action on Programs page, not replacing existing FAB | Template selection is an alternative creation path. Adding it alongside the existing "Create Program" manual flow preserves both options. Template Drawer uses vaul-svelte Drawer (D079). Premium templates NOT shown in onboarding — only on Programs page for existing users. | Yes — could integrate into create flow if UX feedback prefers it |
| D084 | M003/S04 | pattern | Template registry split | Separate `PROGRAM_TEMPLATES` (3 free) and `PREMIUM_PROGRAM_TEMPLATES` (5 premium) + combined `ALL_TEMPLATES` (8) | Onboarding imports `PROGRAM_TEMPLATES` (D015) which must remain free-only. Template browsing UI uses `ALL_TEMPLATES`. Separate exports prevent accidental premium template leakage into onboarding. | No |
| D085 | M003/S04 | data | Premium template data as English strings | Template names/descriptions are hardcoded English in data files, matching free template pattern | Free templates (`ppl.ts`, `upper-lower.ts`, `full-body.ts`) use hardcoded English strings for `name` and `description`. Premium templates follow same pattern. Template names/descriptions are not i18n'd — they're training methodology identifiers. UI chrome around them (section headers, buttons, badges) IS i18n'd. | Yes — if template name localization is requested |
| D086 | M004 | arch | Sync engine choice | PowerSync self-hosted (Open Edition) over custom REST sync or ElectricSQL | PowerSync is purpose-built for offline-first SQLite sync. Handles conflict resolution (LWW per field), incremental delta sync, offline queue, reconnect. Self-hosted Open Edition is free, same codebase as cloud. ElectricSQL doesn't handle client-side persistence. Custom REST sync would take months. | Yes — could switch to PowerSync Cloud if self-hosting proves too complex |
| D087 | M004 | arch | Auth provider choice | Better Auth with JWT + Bearer plugins over Lucia (deprecated) or Clerk/Auth0 | Better Auth provides JWT/JWKS (which PowerSync needs), Bearer tokens for mobile API auth, SvelteKit integration. Production-proven in reference app (`references/capacitor-live-updates`). Lucia is deprecated. Clerk/Auth0 add external dependency + cost. | No |
| D088 | M004 | arch | Server-side ORM | Drizzle ORM with PostgreSQL | Type-safe queries, auto-generates Better Auth schema, migration tooling. Already proven in reference app with Better Auth adapter. Same Postgres instance for auth and app data. | No |
| D089 | M004 | arch | Backend host | SvelteKit `apps/web` scaffold with `adapter-node` | Already exists in monorepo, same UI framework, adapter-node already configured. Adding API routes is natural. Reference app demonstrates exact pattern. No separate API server needed. | No |
| D090 | M004 | arch | SQLite plugin swap strategy (supersedes D016) | `@powersync/capacitor` replaces `@capgo/capacitor-fast-sql` entirely | PowerSync manages its own SQLite DB file. Cannot run both plugins side-by-side. Migration is all-or-nothing. PowerSync uses `capacitor-community/sqlite` internally on native. The thin `dbExecute`/`dbQuery` wrapper pattern means only `database.ts` + 5 repositories + test helper need adaptation. SQL stays the same. | No |
| D091 | M004 | pattern | Soft-delete sync translation | `uploadData` connector translates PowerSync DELETE ops to soft-delete UPDATEs on server | PowerSync default: "deletes always win." App uses soft-delete (`deleted_at` timestamp). The CRUD API must interpret DELETE operations as `UPDATE SET deleted_at = NOW()`, not `DELETE FROM`. Otherwise data loss. | No |
| D092 | M004 | pattern | Exercise seed data strategy | Stable UUIDs for seed exercises, shared bucket in sync rules | Seed exercises use deterministic UUIDs (derived from exercise name). Server seeds the same exercises with same UUIDs. Sync rules include a shared "global exercises" bucket for seed data + per-user bucket for custom exercises. Prevents duplication on first sync. | No |
| D093 | M004 | pattern | Auth token storage on mobile | `@capacitor/preferences` for Bearer token, same as premium state | Follows existing pattern (D034, D075). Simple get/set. Token stored after sign-in, cleared on sign-out. PowerSync connector's `fetchCredentials()` reads from Preferences. | No |
| D094 | M004 | pattern | Offline-to-account transition | PowerSync local-only mode until sign-up, then `connect()` uploads all local data | App works without account (current behavior preserved). On sign-up, PowerSync's `connect()` triggers initial upload of all local data via `uploadData` connector. No data loss during transition. | No |
| D095 | M004 | pattern | Backup/restore via sync | Full sync = backup; new device sign-in = restore | No separate backup/restore mechanism. Signing in on a new device triggers full sync from PowerSync Service, which pushes all user data. This IS the restore. Simplest possible implementation that meets the requirement. | No |
| D096 | M004 | scope | Server-side receipt validation | Deferred — not an Active requirement | CR004 from research. Natural addition but adds scope. M003's D071 local-only validation is acceptable. Can be added opportunistically if S01 backend work makes it trivial, otherwise deferred. | Yes — add when IAP fraud is a concern |
| D097 | M004 | scope | SvelteKit experimental remoteFunctions | Avoid — use standard `+server.ts` API routes | Reference app uses `remoteFunctions: true` and `command()`/`query()` from `$app/server`. These are experimental and may change. Standard `+server.ts` API routes are stable and well-understood. Safer choice for a production sync API. | Yes — adopt if APIs stabilize |
| D098 | M004 | arch | Sync engine choice (supersedes D086) | Custom REST sync over PowerSync or ElectricSQL | PowerSync replaces the SQLite driver entirely with its own managed DB — unacceptable, we want to keep `@capgo/capacitor-fast-sql` and raw SQLite control. Custom REST sync uses existing `updated_at`/`deleted_at` columns for change tracking, incremental push/pull, and LWW conflict resolution. More implementation work but zero external runtime dependencies and full control over the data layer. | No |
| D099 | M004 | arch | Keep existing SQLite layer (supersedes D090) | `@capgo/capacitor-fast-sql` stays, no plugin swap | PowerSync would replace the SQLite plugin entirely with its own managed DB and internal table structure. Instead, keep the existing plugin, existing `database.ts` wrapper, existing repositories, and existing 428 tests untouched. Sync is a new layer on top, not a replacement underneath. | No |
| D100 | M004 | pattern | Sync protocol design | Incremental push/pull with `updated_at` high-water mark, LWW per row | Client tracks `last_push_at` and `last_pull_at` per table in Preferences. Push: send rows where `updated_at > last_push_at`. Pull: request rows where server `updated_at > last_pull_at`. Server compares `updated_at` on push for LWW. Simple, no CRDT complexity needed for single-user data. | Yes — if clock skew proves problematic, add server-side timestamping |
| D101 | M004 | pattern | Soft-delete in sync (supersedes D091) | Soft-deleted rows sync normally via `updated_at` | Since we control the sync protocol, soft-deleted rows (`deleted_at IS NOT NULL`) are just regular rows with an `updated_at` change. They push/pull like any other update. No special translation needed — the `deleted_at` column value syncs as-is. | No |
| D102 | M004 | pattern | Seed exercise identity (supersedes D092) | Deterministic UUIDs for seed exercises derived from name hash | Switch from `crypto.randomUUID()` to a deterministic UUID v5 (or similar) derived from exercise name. All devices produce identical seed exercise UUIDs. Schema v6 migration re-IDs existing seed exercises and updates FK references. Server seeds with same deterministic UUIDs. Eliminates dedup problem entirely. | No |
| D103 | M004 | pattern | Auth token storage on mobile (supersedes D093) | `@capacitor/preferences` for Bearer token, same pattern as premium state | Follows existing pattern (D034, D075). Simple get/set. Token stored after sign-in, cleared on sign-out. Sync service reads token from Preferences for API auth headers. | No |
| D104 | M004 | pattern | Offline-to-account transition (supersedes D094) | Full push of all local data on first sign-in | App works without account (current behavior preserved). On first sign-in, sync service does a full push of all local data. Server receives it all, applies LWW (all new data wins since server has nothing). No data loss during transition. | No |
| D105 | M004 | pattern | Backup/restore via sync (supersedes D095) | Full sync = backup; new device sign-in = restore | No separate backup/restore mechanism. Signing in on a new device triggers full pull from server, which returns all user data. This IS the restore. Simplest implementation that meets the requirement. | No |
| D106 | M004 | pattern | Sync trigger strategy | After sign-in (full), on app resume, on connectivity change, manual | Sync runs automatically at key moments: full sync after sign-in, incremental on app resume and connectivity restore. Manual "Sync Now" button in UI. No real-time WebSocket — polling on natural app lifecycle events is sufficient for single-user training data. | Yes — add periodic timer if freshness matters more |
| D107 | M004 | pattern | Sync tables scope | 7 user tables sync; exercises are hybrid (seed=shared, custom=user-owned) | Tables synced: programs, training_days, exercise_assignments, mesocycles, workout_sessions, workout_sets, body_weight_entries. Exercises: seed exercises (is_custom=0) are NOT synced (identical on all devices via deterministic UUIDs). Custom exercises (is_custom=1) sync normally. schema_version is local-only. | No |
| D108 | M004/S01 | pattern | Mobile-to-API base URL configuration | Hardcoded constant in auth-client.ts, environment-specific at build time later | Mobile SPA (static adapter) cannot read server env vars at runtime. API base URL is a constant string in `auth-client.ts` defaulting to `http://localhost:5174` for dev. Production value set during build or via a config module. No complex env system — just a string constant that works now and can be extracted later. | Yes — extract to build-time env when deploying |
| D109 | M004/S01 | pattern | Server-side app schema user_id strategy | `user_id` column on every app table, NOT NULL, FK to auth user.id | Server-side Postgres tables mirror client-side SQLite schema but add `user_id` to scope all data per user. NOT NULL constraint — every server row belongs to a user. Client-side SQLite has no `user_id` (single-user device). Sync service attaches `user_id` on push. | No |
| D110 | M004/S01 | pattern | JWT plugin adds jwks table to schema | Better Auth JWT plugin requires a `jwks` Postgres table for key storage | JWT plugin stores JWKS keys in DB. Schema has 13 tables total: 5 auth (user, session, account, verification, jwks) + 8 app. The jwks table is auto-managed by Better Auth — no manual key rotation needed. | No |
| D111 | M004/S01 | pattern | Auth session resolution with try-catch | `auth.api.getSession()` wrapped in try-catch in hooks | Better Auth's `getSession()` can throw on malformed cookies/tokens. Without try-catch, any auth resolution failure crashes the entire request. Wrapping in try-catch lets the request proceed unauthenticated, and the API guard handles rejection. | No |
| D112 | M004/S01 | pattern | Bearer token extraction from `set-auth-token` header | Mobile auth-client reads token from `set-auth-token` response header (Bearer plugin output), falls back to body `token` field | Better Auth Bearer plugin's after hook extracts the session token from `Set-Cookie` and exposes it via `set-auth-token` response header + `Access-Control-Expose-Headers`. This is the signed token suitable for Bearer auth. Body `token` is a fallback. Mobile stores this token in Preferences and sends as `Authorization: Bearer <token>`. | No |
| D113 | M004/S01 | pattern | Mobile auth service uses raw fetch, not Better Auth client SDK | Direct fetch to Better Auth REST endpoints instead of `better-auth/svelte` client | Mobile SPA uses static adapter and calls a separate API server. The Better Auth client SDK is designed for same-origin SvelteKit apps with cookie-based auth. For cross-origin Bearer token flows, raw fetch with explicit token management is simpler and more transparent. | Yes — reconsider if Better Auth adds a mobile-friendly client |
| D114 | M004/S02 | pattern | Schema migration system: code-based migrations after SQL | `applySchema()` runs idempotent DDL from schema.sql then executes versioned code migrations (v6+) | Schema.sql handles CREATE TABLE/INDEX idempotently. Code migrations handle data transformations (re-IDing exercises, cascading FK updates) that can't be expressed in pure DDL. Version check guards each migration. Scales to future schema changes without a migration framework. | Yes — if migration count exceeds ~15 |
| D115 | M004/S02 | pattern | UUID v5 implementation: zero-dependency SubtleCrypto | UUID v5 via `crypto.subtle.digest('SHA-1', ...)` with RFC 4122 URL namespace, no external package | SubtleCrypto is available in all target runtimes (browser, Node 18+, native via JSC/V8). ~15 lines. Same function used on client (seed + migration) and server (seed + sync). Produces identical UUIDs across all environments. | No |
| D116 | M004/S02 | pattern | Seed exercise sync strategy: per-user copies on server | Each user gets their own copy of seed exercises in Postgres with their `user_id` | Simpler than a shared seed bucket. On first push, user's seed exercises INSERT with their `user_id`. On pull from another device, they receive these rows. Deterministic UUIDs ensure IDs match across devices. No special system-user or shared-bucket logic needed. | Yes — if storage becomes a concern with many users |
| D117 | M004/S02 | pattern | Sync error handling: catch-and-log, never throw | Sync service functions catch all errors and log with `[Sync]` prefix, never throw | Sync is a background operation — failures must not crash the app or block UI. Silent failure + retry on next lifecycle trigger is the correct behavior. Sync state timestamps not updated on failure, so next trigger retries the same range. | No |
| D118 | M004/S02 | pattern | Dynamic SQL via Drizzle sql template tag | `sql.identifier()` for dynamic table/column names, parameterized values for data | Avoids raw string interpolation in SQL. Sync table registry is the single source of truth for valid table/column names. Generic push/pull logic works across all 8 tables without per-table SQL. | No |
| D119 | M004/S02 | pattern | body_weight_entries sync upsert strategy | Server: catch Postgres 23505 + fallback UPDATE. Client: 3-step (match by id → match by date → insert) | Partial unique index on `(date) WHERE deleted_at IS NULL` requires special handling on both sides. Server catches the constraint violation and retries as update. Client does a pre-check to avoid the conflict entirely. | No |
| D120 | M004/S02 | pattern | Server timestamps as sync high-water marks | `server_now` from response used as next `last_push_at`/`last_pull_at`, never client clock | Eliminates client clock skew from sync state tracking. The server's `NOW()` is authoritative for determining which rows have been synced. Row-level `updated_at` still uses client timestamps for LWW comparison. | No |
| D121 | M004/S03 | pattern | CSV export format: denormalized workout log, not per-table dumps | Single denormalized CSV joining sessions/sets/exercises/programs/training_days | Users want to open workout history in a spreadsheet and see meaningful data. Raw per-table dumps with UUID FKs are useless without joining. Denormalized log (Date, Program, Training Day, Exercise, Set #, Set Type, Weight, Reps, RIR, Completed) is immediately useful. Body weight is a separate simple CSV. | No |
| D122 | M004/S03 | pattern | No external CSV library | Template strings + RFC 4180 escape function (~20 lines) | Data is flat, predictable rows. PapaParse and similar libraries are 30KB+ and designed for parsing (which we don't need). A small `escapeCSV()` function handles the spec correctly for our simple output case. | Yes — if export formats grow complex |
| D123 | M004/S03 | pattern | CSV headers in English, not localized | Column headers are fixed English strings for data portability | CSV files may be imported into other tools, spreadsheets, or scripts. Localized headers would break downstream consumers. Consistent with D085 (template data as English strings). UI labels around export ARE localized. | No |
| D124 | M004/S03 | pattern | Export file handling: Filesystem + Share plugins with web Blob fallback | `@capacitor/filesystem` writes to Cache dir, `@capacitor/share` invokes share sheet. Web: Blob + `<a download>` | Standard Capacitor core plugin pattern. Cache dir for ephemeral files (OS manages cleanup). Share plugin's `files` parameter only works on iOS/Android, so web needs a different path. Blob download is functional for dev/testing. | No |
| D125 | M004/S04 | pattern | Sync error tracking in logError() | Error capture in shared logError() instead of orchestration catch blocks | pushChanges/pullChanges internally catch and swallow their errors — orchestration-level try/catch never fires for sync failures. logError() captures the error where it actually occurs, populating syncState.lastError and lastErrorAt for UI display. | No |
| D126 | M004/S04 | pattern | In-memory sync state + Preferences merge | getSyncState() merges in-memory flags with Preferences timestamps | isSyncing/lastError/lastErrorAt are ephemeral (reset on restart). lastSyncAt is derived from Preferences timestamps (persist across restarts). Single getter merges both sources into a typed SyncState object. Avoids persisting transient state while keeping important timestamps durable. | No |
| D127 | M004/S04 | pattern | Relative time formatting via Intl.RelativeTimeFormat | Inline formatter using Intl.RelativeTimeFormat with paraglide locale, no external library | ~25 lines with 4 thresholds (<60s, <60m, <24h, else days). Uses getLocale() from paraglide for locale-aware formatting. Not extracted to utility — only used in SyncStatusSection. Standard web API, no bundle cost. | Yes — extract if relative time needed elsewhere |
| D128 | M004/S04 | pattern | Sign-out clears sync timestamps | clearSyncState() called in handleSignOut after successful authSignOut() | Prevents cross-account data leak where User A's sync timestamps would persist for User B. clearSyncState() removes Preferences keys (sync_last_push_at, sync_last_pull_at) and resets in-memory state. Called before authState reset to ensure clean transition. | No |
